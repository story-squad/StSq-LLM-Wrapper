from setuptools import setup

setup(
   name='StSqLLMWrapper',
   version='0.0.1',
   description="""An interface that wraps LLM's APIs""",
   author='Tasha Upchurch, Jeannine Proctor, Darwin Johnson, ',
   author_email='TashaSkyUp@gmail.com',
   packages=['StSqLLMWrapper'],
   install_requires=[], #external packages as dependencies
   scripts=[]
)